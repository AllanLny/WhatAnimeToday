// imageProxy helper was added for a short-lived optimization experiment
// Currently neutralized — remove or reintroduce if you plan to run imgproxy locally.
export function imgProxyUrl() { return ''; }
export function buildSrcSet() { return ''; }
export default { imgProxyUrl, buildSrcSet };
